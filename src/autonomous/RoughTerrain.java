package autonomous;

public class RoughTerrain {
	/*
	 * Notes from observing matches
	 * 
	 * The robot seems to veer to the right whenever crossing 
	 * the rough terrain.
	 */
	public static void cross(int position){
		
	}
}
